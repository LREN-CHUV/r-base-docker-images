r-jdbc:
  build: Dockerfile
  image: hbp/r-database
  pre:
    - echo "Download JDBC jars"
    - ./download-jdbc-jars.sh
    - echo "Preparing r-database"
  post:
    - echo "Finished r-database"

r-database-test:
  build: Dockerfile.test
  image: hbp/r-database-test
  pre:
    - echo "Preparing r-database-test"
  post:
    - echo "Finished r-database-test"
  test:
    - tests/setup.sh
    - docker run --rm --link test-postgres:postgres hbp/r-database-test
    - tests/teardown.sh

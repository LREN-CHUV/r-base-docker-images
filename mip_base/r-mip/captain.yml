r-base:
  build: Dockerfile
  image: registry.federation.mip.hbp/mip_base/r-mip
  pre:
    - echo "Preparing r-mip"
  post:
    - echo "Finished r-mip"

r-base-test:
  build: Dockerfile.test
  image: registry.federation.mip.hbp/mip_base/r-mip-test
  pre:
    - echo "Preparing r-mip-test"
  post:
    - echo "Finished r-mip-test"
  test:
    - docker run --rm registry.federation.mip.hbp/mip_base/r-mip-test test

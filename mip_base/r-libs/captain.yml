r-libs:
  build: Dockerfile
  image: registry.federation.mip.hbp/mip_base/r-libs
  pre:
    - echo "Preparing r-libs"
  post:
    - echo "Finished r-libs"

r-libs-test:
  build: Dockerfile.test
  image: registry.federation.mip.hbp/mip_base/r-libs-test
  pre:
    - echo "Preparing r-libs-test"
  post:
    - echo "Finished r-libs-test"
  test:
    - docker run --rm registry.federation.mip.hbp/mip_base/r-libs-test test

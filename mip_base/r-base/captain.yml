r-base:
  build: Dockerfile
  image: registry.federation.mip.hbp/mip_base/r-base
  pre:
    - echo "Preparing r-base"
  post:
    - echo "Finished r-base"
  test:
    - docker run --rm registry.federation.mip.hbp/mip_base/r-base test

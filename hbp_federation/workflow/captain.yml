workflow:
  build: Dockerfile
  image: registry.federation.mip.hbp/hbp_federation/workflow
  pre:
    - echo "Preparing workflow"
    - ./download-src.sh
    - docker pull registry.federation.mip.hbp/hbp/r-database:latest
  post:
    - echo "Finished workflow"
